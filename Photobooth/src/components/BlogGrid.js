import React from "react";

class BlogGrid extends React.Component {
    /*componentDidMount() {
        const blogs = fetch("/API/")
            .then(data => data.json())
            .then(posts => {
                console.log(posts);
            });
    }*/
    render() {
        return <h1>Blog</h1>;
    }
}

export default BlogGrid;
